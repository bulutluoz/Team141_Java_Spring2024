package day46_collections;

import java.util.Deque;
import java.util.LinkedList;
import java.util.List;
import java.util.Queue;

public class C01_LinkedList {

    public static void main(String[] args) {

        // List<Integer> list = new List(); // 'List' is abstract; cannot be instantiated

        // Queue<Integer> queue = new Queue(); // 'Queue' is abstract; cannot be instantiated

        // Deque<Integer> deque = new Deque(); // 'Deque' is abstract; cannot be instantiated


        List<Integer> list = new LinkedList<>();

        Queue<Integer> queue = new LinkedList<>();

        Deque<Integer> deque = new LinkedList<>();

        LinkedList<Integer> linkedList = new LinkedList<>();



    }
}
